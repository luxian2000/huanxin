# QAE083_GPU 量子自编码器改进方案（可操作版）

> 目标：提升模型输出概率分布与目标分布的一致性，改善重构质量，并评估强纠缠层的适配性。

## 1. 现状诊断（必须先做）

### 1.1 多样本评估（替代单样本结论）
- 动作：在测试集中随机抽取 200~1000 个样本，统计 MSE、KL、JSD、Hellinger、相关系数分布。
- 目的：判断当前模型是否普遍偏差或仅个例异常。
- 输出：保存均值/方差/分位数统计，并画出直方图（可用 matplotlib）。

### 1.2 目标分布检查
- 动作：对测试样本的目标分布统计熵值、最大概率值、Top-K 稀疏性。
- 目的：确认目标分布是否过于“平坦”，导致学习难度提升。

### 1.3 输出分布检查
- 动作：统计输出分布的熵、Top-K 稀疏性、分布偏斜度。
- 目的：判断量子电路是否输出了“过平/过尖”的分布。

---

## 2. 损失与目标匹配改进（优先级最高）

### 2.1 损失函数改造（建议组合）
- 方案 A：`loss = α * KL + (1-α) * MSE`，建议 α=0.7 起步。
- 方案 B：使用 `JSD` 作为主损失，更稳定且对称。
- 方案 C：加入熵正则项，避免输出分布过平：`loss += λ * H(output)`，建议 λ=1e-3。

### 2.2 目标分布重构
- 将输入向量归一化前先进行 **温度缩放**：`x^(1/T)`，T=0.5~2.0 搜索。
- 目的：避免目标分布过平，增加可辨识结构。

---

## 3. 量子电路（Ansatz）验证与对比

### 3.1 强纠缠层对比实验
- 对比三种电路：
  1) StronglyEntanglingLayers
  2) BasicEntanglerLayers
  3) HardwareEfficient（自定义：Rx-Ry-Rz + CNOT）
- 每种电路固定层数 L=2,4,6 对比。
- 指标：KL、JSD、Top-K overlap。

### 3.2 深度与参数初始化
- 将强纠缠层的层数 L 从 4 改为 2 或 6
- 参数初始化改为：均匀分布 `[-0.1, 0.1]`，避免大角度导致梯度噪声。

---

## 4. 训练流程优化（可立即实施）

### 4.1 分阶段训练
- 阶段 1：只训练量子参数（冻结 CsiNet）20 个 epoch。
- 阶段 2：只训练 CsiNet（冻结量子参数）20 个 epoch。
- 阶段 3：联合微调 10~20 个 epoch。

### 4.2 梯度稳定策略
- 对量子参数使用 `grad_clip`：`torch.nn.utils.clip_grad_norm_(dec_params, 1.0)`。
- 学习率调节：量子参数 lr=1e-3，经典编码器 lr=1e-4。

### 4.3 训练样本策略
- 每个 epoch 不仅使用 500 样本，而是随机采样不同的 500 样本。
- 提升训练集覆盖率，避免过拟合某个固定子集。

---

## 5. 量子输出与目标结构对齐

### 5.1 输出归一化确认
- 确保 `qml.probs` 输出后没有额外裁剪。
- 训练时不要强行裁剪 output_probs，避免分布偏移。

### 5.2 加入 softmax 对齐
- 试验：对输出概率加 softmax 再计算损失（保证数值稳定）。

---

## 6. 实验记录与自动化

### 6.1 固定实验配置
- 建议每次实验固定：
  - Ansatz 类型
  - 层数
  - 损失函数组合
  - 温度 T
  - 学习率

### 6.2 结果输出规范
- 每个实验输出：
  - 训练曲线
  - 验证曲线
  - 单样本对比报告
  - 多样本统计表

---

## 7. 建议的优先行动清单（按顺序执行）

1. 多样本统计评估（量化性能）
2. 损失函数替换为 KL+MSE 或 JSD
3. 目标分布温度缩放 T=0.5/1.0/2.0 进行网格搜索
4. 对比 StronglyEntanglingLayers vs BasicEntanglerLayers
5. 分阶段训练与学习率调整

---

## 8. 预期效果

- 相关系数显著提升（>0.3）
- KL/JSD 明显下降
- Top-K overlap 增加
- 输出分布更贴近目标分布

---

## 9. 备注
- 强纠缠层并非“不适合”，问题更可能来自训练方案与目标分布匹配。
- 需要对比实验后才能定性判断。
